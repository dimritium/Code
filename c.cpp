#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int>v;
    v.push_back(5);
    v.push_back(3);
    v.push_back(2);
    v.clear();
    v.push_back(1);
    cout<<(2<<2);
    cout<<v.size();
        return 0;
}
