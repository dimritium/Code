#include <bits/stdc++.h>
#define fl(a,b,c) for(a=b;a<c;a++)
#define pb push_back

using namespace std;

class A
{
	int a;
public:
	void set_a(int key)
	{
		a = key;
	}
};

int main()
{
	A obj;
	obj.set_a(5);
}