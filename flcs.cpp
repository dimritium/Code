#include <bits/stdc++.h>
#define fl(a,b,c) for(a=b;a<c;a++)
#define ll long long int
#define pb push_back
using namespace std;

int main()
{
  int tc, n, m, i, j;
  cin>>tc;
  while(tc--)
  {
      cin>>n>>m;
      char str[n][m];
      fl(i, 0, n)
      {
          fl(j, 0, m)
          {
            cin>>str[i][j];
          }
      }
      
  }
}